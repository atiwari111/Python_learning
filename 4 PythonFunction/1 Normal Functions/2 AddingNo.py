#Function with return

def add(a, b):
    return a + b #return sends data back to where the function was called.
                    #After return, the function stops executing

result = add(3, 5) #function stored in variable "result"
print(result)       # variable has taken as output
